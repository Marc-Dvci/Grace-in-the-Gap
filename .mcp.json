{
  "mcpServers": {
    "grace-in-the-gap": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/mcp/server.js"],
      "env": {
        "GRACE_PLUGIN_ROOT": "${CLAUDE_PLUGIN_ROOT}",
        "GRACE_DATA_DIR": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
